# Aula dia 3/11, Vetores

0x00400000 = códigos = .text

0x10010000 = dados = .data

lw = lw $x (destino), $y   end = [$y] +imm -> da memoria para o mp - da memoria para o mp

sw= sw $$z, imm($k) end = [$y]+ imm  -> do mp para a memoria -> enviar para memoria